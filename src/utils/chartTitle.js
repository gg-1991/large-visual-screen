import ChartTitle from '@/components/chartTitle/index.vue'

const ChartTitleDefault = {
    install: function (Vue) {
      Vue.component('chartTitle', ChartTitle)
    }
}
export default ChartTitleDefault