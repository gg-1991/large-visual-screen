// import { login } from '@/api/usr'
/*export default {
  async login ({ commit, getters }, loginData) {
    if (loginData) {
      commit('login', loginData)
    } else {
      const res = await login()
      const temp = { ...res }
      commit('login', temp)
    }
  },
}*/
